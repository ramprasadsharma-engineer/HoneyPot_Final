# Honeypot Intelligence System - Environment Configuration Example
# Copy this file to `.env` and adjust values as needed.

# API bearer token for authenticating requests to protected endpoints
HONEYPOT_API_TOKEN=demo_token_12345

# Comma-separated list of allowed CORS origins. Use * to allow all (dev only)
HONEYPOT_CORS_ORIGINS=*

# Directory for static assets served at /static
HONEYPOT_STATIC_DIR=static